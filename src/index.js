import React from 'react';
import ReactDOM from 'react-dom';

import './index.css';

import SocialApp from './components/app';

ReactDOM.render(
  <React.StrictMode>
    <SocialApp />
  </React.StrictMode>,
  document.getElementById('root'),
);
